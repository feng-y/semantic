# Semantic Harness v1 Final

Semantic Harness is a Claude Code–first semantic construction plugin.

Scope:
- semantic construction only
- repository understanding
- human-in-the-loop refinement
- accepted semantic baseline generation

Logical commands:
- init
- discover
- refine

Workflow:
discover -> validate -> review -> refine(patch) -> validate -> baseline synthesis

Primary target:
- Claude Code plugin / skill execution model
