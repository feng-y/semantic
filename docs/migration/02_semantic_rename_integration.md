# task package 2 · semantic rename integration

Goal:
- replace `semantic_asset_build` wording with `semantic`
- unify prompts / templates / runner / workspace naming
- keep compatibility notes where needed
