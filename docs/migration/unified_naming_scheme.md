# unified naming scheme

Unified semantic chain:

```text
fact → semantic → demand
```

## Meaning
- fact: existing fact-layer outputs from current semantic pipeline
- semantic: new semantic consolidation layer
- demand: demand card generation layer

## Mapping
Current `discover/review/refine/baseline` outputs are interpreted as `fact`.
New consolidated layer uses `semantic`.
Demand card generation uses `demand`.
