Goal:
Convert `signals.yaml` into `candidates.yaml`.

You must:
1. cluster similar signals
2. give stable candidate names
3. write summary and boundary
4. output exactly four groups:
   - domains
   - concepts
   - rules
   - demand_models
